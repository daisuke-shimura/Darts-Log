<nav class="navbar navbar-expand-md navbar-light bg-white">
  <div class="container-fluid">
    <%= link_to "DartsLog", root_path, class: "navbar-brand" %>

    <% if user_signed_in? %>
      <div class="d-flex align-items-center ms-auto gap-3">

      <%= link_to root_path, class: "d-inline-flex align-items-center text-decoration-none bg-white text-dark border border-2 border-dark rounded-pill px-3 py-2 shadow-sm" do %>
        <div class="d-flex align-items-center">
          <span class="badge bg-secondary me-2">ALL</span>

          <small class="text-muted me-1">Record</small>
          <span class="fw-bold fs-6 me-3"><%= @all_records %></span>

          <small class="text-muted me-1">Game</small>
          <span class="fw-bold fs-6"><%= @all_games %></span>
        </div>

        <div class="vr mx-3 opacity-25"></div>

        <div class="d-flex align-items-center">
          <span class="badge bg-secondary me-2">TODAY</span>

          <small class="text-muted me-1">Record</small>
          <span class="fw-bold fs-6 me-3"><%= @today_records %></span>

          <small class="text-muted me-1">Game</small>
          <span class="fw-bold fs-6"><%= @today_games %></span>
        </div>
      <% end %>

        <div class="d-flex align-items-center">
          <small class="text-muted me-1">ログイン中：</small>

          <div class="fw-semibold me-3">
            <%= current_user.name %>
          </div>

          <%= link_to "ログアウト",
            logout_path,
            data: {
              turbo_method: :delete,
              turbo_confirm: "ログアウトしますか？"
            },
            class: "btn btn-outline-danger btn-sm"
          %>
        </div>

      </div>
    <% else %>
      <div class="ms-auto me-3 text-muted small">
        ログインしていません
      </div>
    <% end %>
  </div>
</nav>